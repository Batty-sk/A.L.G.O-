export const handleTrash = () =>{
    // will take the coordiantes array of a main area then resets it.
    console.log('handle Trash working.. .. . .. .')

}
export const handleChangeMap = () =>{
    // will generate the new map
    console.log(
        'handleChangeMap working. .. . .. .. .'
    )
}

