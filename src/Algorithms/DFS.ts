
// get the canvas ref and manuplate the canvas pixels
export const DFS = (source:number[],target: number[],interval:number,CanvasRef:any)=>
{
    CanvasRef.fillStyle = 'green';
    CanvasRef.fillRect(0, 0, 10, 10);
}   